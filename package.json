{
  "private": true,
  "devDependencies": {},
  "dependencies": {},
  "version": "1.0.12",
  "scripts": {
    "version": "git add package.json",
    "postversion": "git push && git push --tags"
  }
}
